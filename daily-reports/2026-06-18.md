# Daily Report — 2026-06-18

## Headline
Stood up the real product: a `platform/` pnpm monorepo (engine + extractor + eval packages, Fastify API, React/Vite/Tailwind web app) on top of yesterday's script-based pipeline. First repo commit landed (`15230f1`, 93 files, +21,424).

## Platform monorepo (new — `platform/`)
- **pnpm workspace** with shared `tsconfig.base.json`, `CONTRACTS.md`, Dockerfile + `.dockerignore`
- **packages/schema** — shared study/answer types
- **packages/engine** — deterministic screening engine (`engine.test.ts`)
- **packages/extractor** — free-text → structured answers (`extractor.test.ts`)
- **packages/eval** — golden-based eval harness (`eval.test.ts`)
- **apps/api** — Fastify: routes `studies`, `report`, `screen`; libs `studies`, `report`, `engine-shim`, `convo`; `selfcheck` test; vitest
- **apps/web** — React app: components `ScreeningChat`, `VerdictCard`, `FunnelDashboard`, `StudyPicker`, `New/EditStudyModal`, `Header`, `ui/*` (Modal, EmptyState, ErrorBanner, LoadingSpinner); `useTheme`; e2e `verify-flows.mjs`, `verify-visual.mjs`, `verify-api.sh`

## Conversational engine
- POC `poc/converse.mjs` + `poc/wc45726.flow.json` — turn-by-turn `converse` alongside batch `screen`
- Reference docs: `ALLEVIATE-ENGINE-REVERSE-ENGINEERED.md`, `CONVERSATIONAL-ENGINE-POC-AND-PLAN.md`, `PATIENT-CONVERSATION-GAPS.md`, `ENGINE-BUILD-PLAN.html`, `TECH-STACK-AND-ARCHITECTURE.html`

## Studies (2 new → 7 total)
- **C4771002** — study.json + FULL/REDACTED md+html
- **D7960C00015** — study.json + reports; first study built from source PDFs (`protocol.pdf`, `Main_ICF.pdf` → `protocol.txt`, `icf.txt`)
- Backups: `.studies-backup/studies-*.tgz`

## Infra / tooling
- `AZURE-DEPLOY.md`, `.env.example`, `platform/.gitignore`, root `.gitignore` updates
- `evals/extractor-goldens.json` — extractor golden set
- `studygen.mjs` reworked (now 99 KB); legacy `ui/` (app.js/index.html/style.css) committed

## State
- Working tree clean; everything in single commit `15230f1`
- Tests present across engine/extractor/eval/api but not run in this session — **verify `pnpm -r test` green before next push**
